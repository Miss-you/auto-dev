<!-- No requirement-level changes. This change implements the existing run-lifecycle spec as-is. -->
<!-- The spec was updated in a prior session (added scenarios for aborted, cycle limit, verified→failed handling). -->
<!-- Those updates are already reflected in openspec/specs/run-lifecycle/spec.md. -->
